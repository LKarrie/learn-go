package gooperator

func GoOperator() string {

	//运算符
	// + - * / %
	// == != > >= < <=
	// && || !

	// 位运算符
	// & 两位为1 才为1
	// | 两位有一个为1 就为1
	// ^ 两位不同 为1
	// <<  a << b a各位左移b位 高位丢低位补0
	// >> a >> b a各位左移b位

	// = += -= *= /= %= <<= >>= &= |= ^=

	return "learn-go-operator"

}
